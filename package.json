{
  "name": "choodo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "budo index.js:bundle.js --live --open -H localhost",
    "build": "browserify index.js -t [ babelify --presets [ es2015 ] ] | uglifyjs > bundle.js",
    "deploy:upload": "surge ./ choodo.surge.sh",
    "deploy": "npm run build && npm run deploy:upload"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "choo": "^4.1.0"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.22.0",
    "babelify": "^7.3.0",
    "browserify": "^14.1.0",
    "budo": "^9.4.7",
    "uglify-js": "^2.8.11"
  }
}
